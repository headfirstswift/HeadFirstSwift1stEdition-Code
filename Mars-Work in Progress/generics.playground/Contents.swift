
// Basic generics (using types instead of values and the benefits that brings)

// The Swift type checker can reason about your code

// Values defined by other values

// Clumsy enum with two cases example

// Modelling something more complex

// Varying the types themselves to do something

// Generic types

// Type parameters

// Type constraints

// Using Array with its proper <> syntax (instead of [])

// Dictionary in Swift is a good example of using generics with protocols to model complex datastructures

// Optionals are also a generic

// Generic function parameters

